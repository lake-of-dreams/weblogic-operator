apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  ports:
  - port: 7001
    name: mysql
  - port: 17001
    name: mysql-replication
  clusterIP: None
  selector:
    app: mysql
